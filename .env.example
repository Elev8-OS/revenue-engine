# Names only. Values belong in Railway variables, never in this file, a commit or a log.
DATABASE_URL=
ELEV8_API_BASE=
ELEV8_API_TOKEN=
PRICELABS_API_KEY=
CHANNEX_API_KEY=
MDV_API_BASE=https://app.mydatavalue.com/api/v1
MDV_CLIENT_ID=
# Seed only. Used once to populate oauth_token, then the database is the truth —
# the token rotates, so a variable holding one goes stale and a redeploy that
# replayed it would revoke the whole grant.
MDV_BOOTSTRAP_REFRESH_TOKEN=
CHANNEX_WEBHOOK_SECRET=
APP_SESSION_SECRET=
ALLOWED_EMAILS=
