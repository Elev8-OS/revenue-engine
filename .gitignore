node_modules/
dist/
.env
.env.*
!.env.example
*.log

# Not committed yet: the lockfile is generated on the first local install.
# Run `npm install` and commit it — reproducible installs depend on it.
package-lock.json
